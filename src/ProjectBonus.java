public interface ProjectBonus {
    //Create a interface ProjectBonus

    //Create a calculateProject that will be used in what ever class implements this interface
    double calculateProjectBonus();
}
